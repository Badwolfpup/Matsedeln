<Page x:Class="Matsedeln.Pages.IngredientPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:inter="http://schemas.microsoft.com/xaml/behaviors"
      xmlns:local="clr-namespace:Matsedeln.Pages"
      xmlns:vwp="clr-namespace:WpfToolkit.Controls;assembly=VirtualizingWrapPanel"      
      xmlns:converter="clr-namespace:Matsedeln.Converters"
      xmlns:usercontrols="clr-namespace:Matsedeln.Usercontrols"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="IngredientPage">
    <Page.Resources>
        <local:GoodsnameConverter x:Key="GoodsnameConverter"></local:GoodsnameConverter>
        <converter:ImageUrlConverter x:Key="ImageUrlConverter"></converter:ImageUrlConverter>
        <converter:SelectedBorderConverter x:Key="SelectedBorderConverter"></converter:SelectedBorderConverter>
    </Page.Resources>
    <inter:Interaction.Triggers>
        <inter:EventTrigger EventName="Loaded">
            <inter:InvokeCommandAction Command="{Binding LoadGoodsCommand}"/>
        </inter:EventTrigger>
    </inter:Interaction.Triggers>
    <Grid Background="Cornsilk">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Margin="10 0 0 0" Background="Cornsilk" Grid.Column="1" Grid.ColumnSpan="2" Orientation="Horizontal">
            <Button Content="Recept" Command="{Binding ShowRecipesPageCommand}"></Button>
            <Button Content="Maträtter" Command="{Binding ShowDishesPageCommand}"></Button>
            <Button Content="Kalender" Command="{Binding ShowMenuPageCommand}" ></Button>
            <TextBox Margin="15 0 15 0" Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"/>

        </StackPanel>
        <Button Grid.Column="2" HorizontalAlignment="Right" Width="35" Height="35" Style="{StaticResource BtnNoMax}" 
                BorderBrush="Transparent" BorderThickness="1" Background="PaleVioletRed" Command="{Binding DeleteGoodCommand}">
            <Button.Content>
                <TextBlock FontSize="16" FontFamily="Segoe MDL2 Assets" Text="&#xF8AA;"></TextBlock>
            </Button.Content>
            <Button.ToolTip>
                <ToolTip ToolTipService.InitialShowDelay="200" ToolTipService.ShowDuration="2000">
                    <TextBlock>Radera</TextBlock>
                </ToolTip>
            </Button.ToolTip>
        </Button>
        <Grid x:Name="LoadingOverlay" Grid.Row="1" Visibility="{Binding IsLoading}" Background="#80000000">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <usercontrols:LoadingSpinner Width="80" Height="80" SpinnerColor="White"/>
                <TextBlock Text="Loading data..." 
                      Foreground="White" 
                      FontSize="16" 
                      Margin="0,20,0,0"
                      HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
        <ListBox Grid.Row="1" 
             x:Name="GoodsListBox" 
             ItemsSource="{Binding GoodsViewSource.View}"
             VirtualizingPanel.IsVirtualizing="True"
             VirtualizingPanel.VirtualizationMode="Recycling"
             VirtualizingPanel.CacheLengthUnit="Item"
             VirtualizingPanel.CacheLength="10,10"
             VirtualizingPanel.ScrollUnit="Pixel"
             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
             ScrollViewer.VerticalScrollBarVisibility="Auto"
             SelectionMode="Single"
             SelectedItem="{Binding SelectedGood}"
             Background="Transparent"
             BorderThickness="0">

            <ListBox.Resources>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.Resources>

            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <vwp:VirtualizingWrapPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="{Binding IsHighlighted, Converter={StaticResource SelectedBorderConverter}}" 
                BorderThickness="3" 
                Margin="5" 
                Width="170" 
                Padding="5" 
                CornerRadius="10">
                        <inter:Interaction.Triggers>
                            <inter:EventTrigger EventName="MouseLeftButtonDown">
                                <inter:InvokeCommandAction 
                        Command="{Binding DataContext.SelectBorderCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" 
                        CommandParameter="{Binding}"/>
                            </inter:EventTrigger>
                        </inter:Interaction.Triggers>
                        <StackPanel>
                            <Image Width="100"
                        Height="100"
                        Source="{Binding ImagePath, Converter={StaticResource ImageUrlConverter}, IsAsync=True}"/>
                            <TextBlock HorizontalAlignment="Center"
                            FontSize="18"
                            Text="{Binding Name, Converter={StaticResource GoodsnameConverter}}"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

       
    </Grid>
</Page>

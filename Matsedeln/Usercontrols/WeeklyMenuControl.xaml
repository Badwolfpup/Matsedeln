<UserControl x:Class="Matsedeln.Usercontrols.WeeklyMenuControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Matsedeln.Usercontrols"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="650">

    <Grid Background="#FFFDFDFD">
        <StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="4*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="&lt;" Style="{StaticResource BtnNoMax}" Width="30" Click="PrevWeek_Click"></Button>
                <TextBlock Grid.Column="1" FontSize="20" Text="{Binding Weekanddate}"/>

                <Button Grid.Column="2" Content="&gt;" Style="{StaticResource BtnNoMax}" Width="30" Click="NextWeek_Click"></Button>
            </Grid>
            <ItemsControl Margin="0 20 0 0" ItemsSource="{Binding MenuListSource}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding}"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"></ColumnDefinition>
                                    <ColumnDefinition Width="3*"></ColumnDefinition>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"></RowDefinition>
                                    <RowDefinition Height="*"></RowDefinition>
                                </Grid.RowDefinitions>
                                <TextBlock Text="Lunch" HorizontalAlignment="Right" Grid.Column="0" Grid.Row="0"/>
                                <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding main.RecipeList}" SelectionChanged="Lunch_SelectionChanged">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Image Width="50" Height="50" Source="{Binding Imagepath}"></Image>
                                                <TextBlock Text="{Binding Name}"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                                <TextBlock Text="Middag" HorizontalAlignment="Right" Grid.Column="0" Grid.Row="1"/>
                                <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding main.RecipeList}" SelectionChanged="Dinner_SelectionChanged">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Image Width="50" Height="50" Source="{Binding Imagepath}"></Image>
                                                <TextBlock Text="{Binding Name}"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </Grid>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
</UserControl>

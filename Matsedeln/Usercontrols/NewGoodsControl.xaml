<UserControl x:Class="Matsedeln.Usercontrols.NewGoodsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:inter="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:local="clr-namespace:Matsedeln.Usercontrols"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="800">



        <Grid Background="Cornsilk">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
            
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center">

            <Button Grid.Column="0" Command="{Binding ShowAddRecipeUserControlCommand}">
                <Button.Content>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="16" Text="&#xF8AA;"/>
                        <TextBlock Text=" recept" FontWeight="Bold" />
                    </StackPanel>
                </Button.Content>
            </Button>
        </StackPanel>
        <StackPanel Grid.Row="1" Margin="5 10 0 0">
            <Image 
                x:Name="BoundImage"
                Source="{Binding GoodsImage}"
                Grid.Row="0" 
                Stretch="Uniform"
                HorizontalAlignment="Center"
                Width="250" Height="250" 
                MinHeight="40" MinWidth="40">
                <inter:Interaction.Triggers>
                    <inter:EventTrigger EventName="MouseLeftButtonDown">
                        <inter:InvokeCommandAction Command="{Binding ImageMouseDownCommand}"/>
                    </inter:EventTrigger>
                </inter:Interaction.Triggers>
            </Image>
            <StackPanel>
                <TextBlock Text="Namn:"/>
                <TextBox Text="{Binding NewGood.Name}"/>

                <TextBlock Text="Gram per dl:"/>
                <StackPanel Orientation="Horizontal">
                    <CheckBox x:Name="CboxGperDL" IsChecked="{Binding IsGperDLEnabled}" Command="{Binding ResetGperDLCommand}" CommandParameter="{Binding ElementName=CboxGperDL, Path=IsChecked}" VerticalContentAlignment="Center"/>
                    <TextBox Text="{Binding GperDL, UpdateSourceTrigger=PropertyChanged}" Width="75" IsEnabled="{Binding ElementName=CboxGperDL, Path=IsChecked}" PreviewTextInput="CheckIfDigit_preview"/>
                </StackPanel>
                <TextBlock Text="Gram per st:"/>
                <StackPanel Orientation="Horizontal">
                    <CheckBox x:Name="CboxGperST" IsChecked="{Binding IsGperSTEnabled}" Command="{Binding ResetGperSTCommand}" CommandParameter="{Binding ElementName=CboxGperST, Path=IsChecked}" VerticalContentAlignment="Center"></CheckBox>
                    <TextBox Text="{Binding GperST, UpdateSourceTrigger=PropertyChanged}" Width="75" IsEnabled="{Binding ElementName=CboxGperST, Path=IsChecked}" PreviewTextInput="CheckIfDigit_preview"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                    <Button Content="{Binding ButtonText}" Width="100" Command="{Binding AddGoodsToDBCommand}" />
                    <Button Content="Avbryt" Width="100" Command="{Binding AbortAddGoodsCommand}" />
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </Grid>

</UserControl>

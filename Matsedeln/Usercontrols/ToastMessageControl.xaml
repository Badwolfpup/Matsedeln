<UserControl x:Class="Matsedeln.Usercontrols.ToastMessageControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Matsedeln.Usercontrols"
             mc:Ignorable="d" 
             x:Name="root"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border x:Name="ToastBorder" 
                MinHeight="50" MaxHeight="100" MaxWidth="250" 
                Background="#333333" CornerRadius="8" Padding="15" 
                Opacity="0" RenderTransformOrigin="0.5,0.5">

            <Border.RenderTransform>
                <TranslateTransform Y="20"/>
            </Border.RenderTransform>

            <Border.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                             From="0" To="1" Duration="0:0:0.3"/>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                             From="20" To="0" Duration="0:0:0.3"/>

                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                             To="0" BeginTime="0:0:3.5" Duration="0:0:0.5"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Border.Triggers>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="&#xE73E;" FontFamily="Segoe MDL2 Assets" 
                           Foreground="LightGreen" VerticalAlignment="Center" Margin="0,0,10,0"/>

                <TextBlock Text="{Binding ElementName=root, Path=Message}" 
                           Foreground="White" FontSize="14" TextWrapping="Wrap"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
